{
  "name": "miller-family",
  "version": "1.0.0",
  "description": "Miller Family — DEL PERRO RP website",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "concurrently \"node --watch server.js\" \"browser-sync start --proxy localhost:3001 --files '**/*.html, **/*.css, js/**/*.js, roles.config.js' --no-open --no-ui --port 3000\""
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "discord.js": "^14.26.4",
    "dotenv": "^16.0.0",
    "express": "^5.2.1",
    "express-session": "^1.19.0",
    "passport": "^0.7.0",
    "passport-discord": "^0.1.4",
    "passport-local": "^1.0.0",
    "session-file-store": "^1.5.0"
  },
  "devDependencies": {
    "browser-sync": "^3.0.4",
    "concurrently": "^10.0.3"
  }
}
